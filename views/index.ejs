<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>LoFo ITB</title>
</head>
<body>
    <ul class="posts">
        <% for(var i=0; i<posts.length; i++) {%>
            <li class="quote">
                <span><%= posts[i].title %></span>
                <span><%= posts[i].description %></span>
            </li>
        <% } %>
    </ul>
</body>
</html>